/// <reference types="@vitest/browser/providers/playwright" />
import { describe, expect, test } from 'vitest';
import { render } from 'vitest-browser-vue';

describe('E2E.Register', () => {
    test('Load App component', async () => {
        // hello world
    });
});
