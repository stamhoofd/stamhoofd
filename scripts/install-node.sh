# First install nvm + reload bash
# install it on drst user, root not needed

# Switch to user that will run dorst

# Install latest node
nvm install node

# Install yarn
npm install -g yarn

# install pm2
yarn global add pm2

# Deploy!