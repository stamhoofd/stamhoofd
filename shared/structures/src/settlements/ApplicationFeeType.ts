/**
 * Which pricing component of an application fee a row represents: the platform's service fee or the
 * passed-through transaction/transfer fee.
 */
export enum ApplicationFeeType {
    Service = 'Service',
    Transfer = 'Transfer',
}
