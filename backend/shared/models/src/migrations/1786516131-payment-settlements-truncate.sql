-- Rows of the unreleased first version: outside development the table is empty, and a resync
-- rebuilds every row with the organizationId the next migration requires
TRUNCATE TABLE `payment_settlements`;
